# Development Dockerfile for Angular
FROM node:18-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm install

# Copy source files (will be overwritten by volume mount)
COPY . .

EXPOSE 4200

# Run Angular dev server with proxy config and host binding for Docker
CMD ["npm", "run", "start", "--", "--host", "0.0.0.0", "--poll", "2000", "--proxy-config", "proxy.conf.json"]

